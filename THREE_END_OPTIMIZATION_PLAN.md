# 城中村消防风险管理系统三端优化方案（居民端 / 网格员端 / 管理员端）

## 1. 目标与原则

基于你当前毕业设计方向（纯网页端），将系统从“通用功能集合”优化为明确的三端系统：
- 居民端：上报、自查、进度、知识接收
- 网格员端：任务处理、巡查、辖区维护、统计
- 管理员端：全局监管、模型配置、知识库与审计

设计原则：
1. **角色边界清晰**：菜单、接口、数据权限三重隔离。
2. **数据闭环可追溯**：上报→分派→处理→反馈→归档全过程日志化。
3. **风险驱动治理**：风险模型输出直接驱动预警、知识推送与督办。
4. **先可用后完善**：优先完成毕业设计必备主流程，再扩展高级能力。

---

## 2. 当前系统能力与需求对照

## 2.1 已具备能力（可复用）
- 用户注册登录、JWT 鉴权、三角色权限框架。
- 隐患上报、分派、处理流程主链路。
- 风险评估接口（区域维度）。
- GIS 地图基础展示（Leaflet + OSM）。
- 知识库推荐、通知日志、操作日志、Excel 导出。

## 2.2 主要缺口（需优化）
1. **居民端缺“个人资料管理”和“仅查看本人上报进度”**。
2. **居民端缺“简易自查清单”与提交记录**。
3. **网格员端缺“巡查计划/巡查记录管理”**。
4. **网格员端缺“辖区信息维护（住宅/居民/周边生活区）”完整页面与流程**。
5. **管理员端缺“风险模型参数可配置页面”与参数持久化**。
6. **管理员端缺“用户审核（特别是网格员账号审核/启停）”流程化能力**。
7. **三端 UI 还需按角色重新编排菜单与仪表盘信息密度**。

---

## 3. 三端目标功能清单（按你的要求细化）

## 3.1 居民端

### A. 账号与资料
- 注册/登录（已具备）
- 个人信息维护：姓名、手机号、居住地址、紧急联系人
- 个人密码修改

### B. 隐患上报与进度
- 表单上报：文字、图片、经纬度、地址、类型
- 我的隐患列表：仅显示本人数据
- 处理进度时间轴：已上报/已分派/处理中/已完成/驳回

### C. 风险可视化
- 城中村底图展示
- 隐患点按状态/类型区分图标颜色
- 点击点位查看详情与处理进度

### D. 信息接收与自查
- 消防知识推荐（按角色+风险）
- 预警消息与处理反馈消息
- 家庭消防简易自查（预设清单打分 + 提交）

---

## 3.2 网格员端

### A. 任务处理
- 待处理任务池（按辖区）
- 处理结果提交（文本+照片）
- 驳回/转派申请（可选）

### B. 巡查管理
- 巡查计划：创建、周期、责任区域
- 巡查记录：签到时间、点位、问题发现、图片
- 计划完成率统计

### C. 辖区基础信息维护
- 住宅信息管理（楼栋、户数、地址）
- 居住人员信息管理（住户关联）
- 周边生活区信息管理（商铺、仓储、重点点位）

### D. 数据统计
- 隐患总数、已办结率、平均处理时长
- 高风险隐患清单
- 超期任务提醒

---

## 3.3 管理员端

### A. 系统配置与权限
- 用户审核、启停、角色分配
- 网格员辖区分配
- 字典配置（隐患类型、处理状态、通知模板）

### B. 隐患流程监控
- 全局隐患看板
- 督办与重新分配
- 超期自动预警阈值配置

### C. 风险模型配置
- 类型权重配置（已有基础表）
- 超期加分规则配置
- 高危附加分配置
- 预警阈值（LOW/MID/HIGH/CRITICAL）可配置

### D. 知识与数据分析
- 知识库文章管理与推送规则
- 多维报表导出（区域、时间、类型、状态）
- 系统日志审计（操作留痕查询）

---

## 4. 数据库优化建议（在现有库上增量）

建议新增以下表（不破坏现有结构）：

1. `self_check_template`：自查模板（题目、分值、风险类型）
2. `self_check_record`：居民自查记录（用户、总分、结论、时间）
3. `self_check_item_record`：自查明细项（是否通过、备注）
4. `patrol_plan`：巡查计划（网格员、区域、周期、状态）
5. `patrol_record`：巡查记录（计划、时间、定位、问题）
6. `risk_model_config`：风险模型参数（可动态生效）
7. `user_audit_record`：用户审核记录（审核人、结果、意见）
8. `area_info`：辖区主数据（区域编码、名称、边界）

关键索引建议：
- `self_check_record(user_id, created_at)`
- `patrol_record(grid_user_id, created_at)`
- `risk_model_config(config_key)`
- `user_audit_record(user_id, created_at)`

---

## 5. 后端接口优化路线

## 5.1 居民端新增
- `GET /api/v1/users/me`
- `PUT /api/v1/users/me`
- `GET /api/v1/hazards/my`
- `GET /api/v1/hazards/{id}/timeline`
- `GET /api/v1/self-check/templates`
- `POST /api/v1/self-check/records`
- `GET /api/v1/self-check/my-records`

## 5.2 网格员端新增
- `GET /api/v1/grid/tasks`
- `POST /api/v1/grid/tasks/{hazardId}/feedback`
- `POST /api/v1/patrol/plans`
- `GET /api/v1/patrol/plans`
- `POST /api/v1/patrol/records`
- `GET /api/v1/patrol/stats`

## 5.3 管理员端新增
- `GET /api/v1/admin/users/pending`
- `POST /api/v1/admin/users/{id}/approve`
- `POST /api/v1/admin/users/{id}/disable`
- `GET /api/v1/admin/risk-model`
- `PUT /api/v1/admin/risk-model`
- `GET /api/v1/admin/dashboard`

---

## 6. 前端三端页面重构建议（纯 HTML + JS）

## 6.1 居民端页面
- `resident-dashboard.html`：我的隐患概览 + 消息
- `resident-report.html`：隐患上报（拍照/定位/描述）
- `resident-progress.html`：进度时间轴
- `resident-self-check.html`：家庭消防自查
- `resident-profile.html`：个人信息维护

## 6.2 网格员端页面
- `grid-dashboard.html`：任务统计与待办
- `grid-task-list.html`：任务接收与处理
- `grid-patrol-plan.html`：巡查计划
- `grid-patrol-record.html`：巡查上报
- `grid-area-maintain.html`：辖区信息维护

## 6.3 管理员端页面
- `admin-dashboard.html`：全局驾驶舱
- `admin-hazard-monitor.html`：流程监控/督办
- `admin-risk-model.html`：模型参数配置
- `admin-knowledge-manage.html`：知识库维护
- `admin-user-audit.html`：用户审核与权限
- `admin-log-audit.html`：操作日志审计

---

## 7. 毕设实现优先级（建议 3 阶段）

## 阶段1（必做，确保可答辩演示）
1. 三端登录后不同菜单与数据权限
2. 居民上报 + 网格员处理 + 管理员督办闭环
3. 风险评估 + GIS 落图 + 基础知识推荐
4. 日志审计 + Excel 导出

## 阶段2（加分项）
1. 居民自查清单
2. 网格员巡查计划与记录
3. 管理员风险模型参数配置页面

## 阶段3（提升项）
1. 消息中心优化（已读/未读）
2. 预警订阅策略与批量推送
3. 驾驶舱多维可视化图表

---

## 8. 答辩展示建议（10~15分钟流程）

1. 管理员登录：展示全局看板和风险热区
2. 居民登录：上报隐患（拍照/定位/描述）
3. 管理员分派：一键分配网格员
4. 网格员处理：上传处理说明与图片
5. 居民查看：进度状态变化与反馈消息
6. 管理员导出：按区域导出治理报表
7. 日志审计：展示谁在何时执行了什么操作

---

## 9. 结论

该优化方案能把现有系统升级为符合你毕设要求的“网页三端协同治理平台”，并且尽量复用你已完成的后端与数据库基础，降低重构成本，保证在答辩前可落地。
